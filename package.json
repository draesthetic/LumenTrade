{
  "name": "chase-trade-analysis",
  "version": "1.0.0",
  "description": "Webapp that turns Zerodha tradebooks into NAV curve, drawdown, and trade statistics.",
  "main": "server.js",
  "scripts": {
    "setup": "npm install --prefix landing",
    "dev": "NODE_ENV=development node server.js",
    "start": "NODE_ENV=production node server.js",
    "build:landing": "npm run setup && npm run --prefix landing build"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  }
}
